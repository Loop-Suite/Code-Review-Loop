name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    uses: Loop-Suite/Code-Review-Loop/.github/workflows/rust-ci-reusable.yml@main
    with:
      msrv: "1.85" # keep in sync with Cargo.toml's `rust-version`
